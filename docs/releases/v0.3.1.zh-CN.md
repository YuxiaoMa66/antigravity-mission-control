# v0.3.1：协调后的稳定交接版本

本修订版本同时通过 GitHub 源码 tag `v0.3.1`、GitHub Release 和公开 npm 包 `0.3.1` 提供。

- 修正可直接交给 Agent 的交接说明，改用已发布的 npm 安装器，不再使用过时的 Git 源码命令。
- 可复现安装示例统一固定到 `@0.3.1` 和 `v0.3.1`。
- 明确 Codex 的最终验收边界：AGY worker 只能在已批准范围内执行。
- 同步 Python、npm、CLI、内置 Skill 检查、发布资产和双语文档。
- 删除过时的 npm `next` 分发标签；稳定 `latest` 标签指向 `0.3.1`。

这是一次打包和文档修订发布。v0.3.0 引入的 Flash 优先路由行为保持不变。

验证包括 Python 和 npm 测试套件、内置 Skill 同步、界面资产检查、安装包预演、隔离安装器 smoke test、GitHub tag 核验和 npm registry 核验。
